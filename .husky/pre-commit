echo "🔍 Running pre-commit checks..."

# Check for secrets in staged files (excluding this hook file and documentation)
echo "🔐 Checking for secrets and API keys..."
FILES_TO_CHECK=$(git diff --cached --name-only | grep -v "\.husky" | grep -v "README" | grep -v "\.md$")
if [ -n "$FILES_TO_CHECK" ]; then
  if echo "$FILES_TO_CHECK" | xargs grep -l "gsk_[a-zA-Z0-9]\{48\}\|sk-[a-zA-Z0-9]\{48\}\|AKIA[A-Z0-9]\{16\}\|mongodb+srv://[^:]*:[^@]*@" 2>/dev/null; then
    echo "❌ ERROR: Potential API key or secret detected in staged files!"
    echo "Please remove secrets before committing."
    exit 1
  fi
fi

# Check for .env files
if git diff --cached --name-only | grep -q "\.env$"; then
  echo "❌ ERROR: .env file detected in commit!"
  echo "Never commit .env files. Use .env.example instead."
  exit 1
fi

# Check for large files (> 50MB)
echo "📦 Checking for large files..."
for file in $(git diff --cached --name-only); do
  if [ -f "$file" ]; then
    size=$(du -m "$file" | cut -f1)
    if [ "$size" -gt 50 ]; then
      echo "❌ ERROR: File $file is ${size}MB (limit: 50MB)"
      echo "Please use Git LFS or exclude from repository."
      exit 1
    fi
  fi
done

# Run lint-staged for code formatting
echo "✨ Running lint-staged..."
npx lint-staged

echo "✅ Pre-commit checks passed!"
